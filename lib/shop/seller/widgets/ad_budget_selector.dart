// Blueprint: docs/shop/shop.md — widgets/AdBudgetSelector
import 'package:flutter/material.dart';

class AdBudgetSelector extends StatelessWidget {
  const AdBudgetSelector({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
