// Blueprint: docs/shop/shop.md — widgets/TaggedProductsPicker
import 'package:flutter/material.dart';

class TaggedProductsPicker extends StatelessWidget {
  const TaggedProductsPicker({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
