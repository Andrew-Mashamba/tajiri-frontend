// Blueprint: docs/shop/shop.md — widgets/PostBoostSection
import 'package:flutter/material.dart';

class PostBoostSection extends StatelessWidget {
  const PostBoostSection({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
