// Blueprint: docs/shop/shop.md — widgets/ShoppablePostCard
import 'package:flutter/material.dart';

class ShoppablePostCard extends StatelessWidget {
  const ShoppablePostCard({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
