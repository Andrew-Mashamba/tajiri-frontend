// Blueprint: docs/shop/shop.md — widgets/LiveReactionsBar
import 'package:flutter/material.dart';

class LiveReactionsBar extends StatelessWidget {
  const LiveReactionsBar({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
