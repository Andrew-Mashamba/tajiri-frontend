// Blueprint: docs/shop/shop.md — widgets/InventoryHistoryCard
import 'package:flutter/material.dart';

class InventoryHistoryCard extends StatelessWidget {
  const InventoryHistoryCard({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
