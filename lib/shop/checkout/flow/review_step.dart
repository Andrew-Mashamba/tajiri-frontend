// Blueprint: docs/shop/shop.md — checkout/flow/review_step.dart
import 'package:flutter/material.dart';

/// Checkout section: totals / price breakdown (`CheckoutScreen` composes content).
class ReviewStep extends StatelessWidget {
  const ReviewStep({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
