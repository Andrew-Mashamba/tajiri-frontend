// Blueprint: docs/shop/shop.md — checkout/flow/confirmation_step.dart
import 'package:flutter/material.dart';

/// Checkout section: trust / escrow copy + budget context before confirm (`CheckoutScreen` composes content).
class ConfirmationStep extends StatelessWidget {
  const ConfirmationStep({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
