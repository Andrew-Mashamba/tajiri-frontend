// Blueprint: docs/shop/shop.md — checkout/flow/payment_step.dart
import 'package:flutter/material.dart';

/// Checkout section: payment method + promo (`CheckoutScreen` composes content).
class PaymentStep extends StatelessWidget {
  const PaymentStep({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
