// Blueprint: docs/shop/shop.md — checkout/flow/shipping_step.dart
import 'package:flutter/material.dart';

/// Checkout section: order lines, delivery address, notes (`CheckoutScreen` composes content).
class ShippingStep extends StatelessWidget {
  const ShippingStep({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
