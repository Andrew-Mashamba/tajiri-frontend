// Blueprint: docs/shop/shop.md — routes/checkout_routes.dart
/// Named routes — mirror `ShopRoutes` / `main.dart`.
abstract final class CheckoutRoutes {
  static const String checkout = '/shop/checkout';
}
