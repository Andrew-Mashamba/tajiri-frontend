// Blueprint: docs/shop/shop.md — widgets/ProductShareMessage
import 'package:flutter/material.dart';

class ProductShareMessage extends StatelessWidget {
  const ProductShareMessage({super.key});
  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
